#include <iostream>
using namespace std;
int  sum(int );
int main() {
    int n,s;
    cout<<"enter the numbers: "<<endl;
    cin>>n;
    s=sum(n);
  cout<<s;
}
int  sum (int n)
{
    if (n==0)
    {
        return 0;
    }
    else
    {
        return(n+sum(n-1));
    }
    }
